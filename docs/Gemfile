source "https://rubygems.org"

# github-pages pins Jekyll + plugins to exactly what GitHub's branch-mode
# Pages build uses, so the Actions workflow and branch mode render identically.
gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
gem "webrick"
